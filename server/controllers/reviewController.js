const reviewModels = require('../models/reviewModels');
const db = require('../models/reviewModels');

const reviewController = {};

reviewController.addReview = (req, res, next) => {

};

module.exports = reviewController;